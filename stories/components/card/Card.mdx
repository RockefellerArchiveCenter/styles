import { Meta, Story } from '@storybook/blocks';
import * as stories from './Card.stories.js';

<Meta of={stories} />

# Card

Cards allow users to quickly browse content of varying types and lengths.

## When to use

Use to display heterogenous content composed of different elements, whose size or
supported actions vary. Allows a collection of multiple data types to be viewed
as a group so that a user can take some action relating to that group.

## Best practices

For accessibility, ensure that card content is placed in the correct reading order.

## Related components

No related components.

## Known issues

No known issues.

## Basic implementation
The number of card columns can be specified in local CSS using media queries,
with the base styles providing one column that takes up the full width of its container.
For example, for 4 card columns on large screens and 3 columns on medium screens, use:
```
.card-list {
  @media screen and (min-width: 36.25em) {
    grid-template-columns: repeat(3, 1fr);
  }

  @media screen and (min-width: 64em) {
    grid-template-columns: repeat(4, 1fr);
  }
}
```

By default, cards are designed to be a list of links. Apply `card__title` to the link element to ensure the
full card is clickable.

<Story of={stories.link} />

## Cards with headings and content sections

There are optional card sections, including a card body and footer to assist with layout. 
When there is extensive content in each card in the list, the titles should be marked up as headings 
with `card__title` applied. The nested link inside the heading does not have a CSS class. 
In this example, the titles are designated as an `<h2>` element, but the **heading level should be adjusted 
based on page structure**.

<Story of={stories.titleLinkWithBodyAndFooter} />

## Using cards as stylized content containers
A card can be used flexibly as a content container that is not
part of a list or a link by adding the `card--container` class and adjusting
the HTML structure for the desired content. Optionally, `card--blue` is available
to adjust the background color. This example includes an `h2`, paragraph, and link text
without the card body, title, or text classes:

<Story of={stories.container} />


